import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
// create todo list in react where we can add a task using an input field and a button "add task", when the task is added, it should contain two buttons i.e edit and delete, the delete button should remove that particular task, and the edit button should let the user edit the task name and update using the save button, when the edit button is clicked, the save button appears and the user can edit the task

);


